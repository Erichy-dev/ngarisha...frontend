import { describe, expect, it } from "vitest";
import ProfilePicVue from "../ProfilePic.vue";

describe("tests admin pic", () => {
  it("tests component is truthy", () => {
    expect(ProfilePicVue).toBeTruthy()
  })
})