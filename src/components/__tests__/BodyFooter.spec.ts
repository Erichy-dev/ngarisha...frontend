// import { flushPromises, mount } from "@vue/test-utils"
import { describe, expect, it } from "vitest";
import BodyFooter from "../BodyFooter.vue";

describe("test footer", () => {
  it("component is truthy", async () => {
    expect(BodyFooter).toBeTruthy();
  });
});
